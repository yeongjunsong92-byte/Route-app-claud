# Route 재작업 진단

## 첨부 시안 기준

지도 시안은 단일 모바일 화면을 기준으로 검색창이 지도 위 상단에 놓이고, 그 아래에 전체·맛집·카페·관광지·숙소 필터가 가로로 배치된다. 지도에는 핑크 하트 마커와 보라색 선택 마커가 있고, 하단에는 장소 리스트가 지도와 분리된 흰색 패널로 놓인다. 장소 상세는 지도 위 하단 카드 형태이며 이미지, 장소명, 평점, 주소, 영업시간, 전화번호, 카테고리, 저장 버튼과 보라색 코스 추가 버튼을 포함한다.

코스 생성 시안은 전체 페이지가 아니라 4개의 독립적인 모바일 화면 흐름이다. 상단에는 뒤로가기, 가운데 코스 만들기, 우측 1/4·2/4·3/4·4/4 진행 표시가 있다. 1단계는 큰 여백과 깃발 아이콘, 코스명 입력, 하단 고정 다음 버튼을 사용한다. 2단계는 검색창·지도·추가된 장소 4개 목록·장소 추가와 다음 버튼을 사용한다. 3단계는 장소별 펼침 카드 안에 방문 시간·머무는 시간·예상 비용·메모를 넣는다. 4단계는 시간순 일정 카드와 총 예상 비용, 이전·저장하기 버튼을 사용한다.

친구·팔로우 시안에는 홈 추천 배너가 아니라 팔로우 홈, 사용자 검색, 사용자 프로필, 공개 코스 목록, 코스 상세, 코스 일정 상세, 내 코스로 저장, 내 버전 코스 수정 화면이 포함된다. 따라서 현재의 단일 홈·코스 모달 구조는 화면 목록을 충족하지 않는다.

## 현재 구현에서 확인된 문제

현재 Home.tsx는 홈을 기본 탭으로 열고, 공개 코스와 코스 상세를 모달로만 표시한다. 첨부 시안처럼 지도 화면을 중심으로 시작하지 않으며, 친구·사용자·공개 코스·내 버전 수정 화면이 독립적인 화면으로 연결되지 않는다. 이전 MapView는 Google Maps 스크립트 오류를 reject하지 않고 지도 컨테이너를 바로 생성해 API 오류가 빈 화면처럼 보일 수 있었다. 새 MapView는 loading/error fallback을 제공해 이 문제를 분리한다.

## 재작업 후 검증

지도 기본 화면은 412px 모바일 뷰에서 검색창·필터 칩·지도 미리보기·주변 장소 바텀 패널·하단 네비게이션 순서로 렌더링되며, Google Maps API가 실패해도 fallback이 표시된다. 콘솔과 네트워크 로그에서 새 런타임 예외는 확인되지 않았고 auth.me와 places.saved 요청은 200으로 응답했다.

내 코스 수정은 `edit-course` 분기를 `renderEditCourse()`로 연결했고, 실제 DB 코스 목록은 숫자형 ID로 정규화한다. 수정 대상은 `courses.get`으로 상세를 불러와 장소 순서·방문 시간·예상 비용·메모를 편집한다. 저장은 `courses.update` mutation 성공 이후에만 성공 토스트와 목록 캐시 갱신을 수행하며, 샘플 코스처럼 DB ID가 없는 항목은 오류 토스트를 표시한다.

TypeScript 검사는 통과했고 Vitest는 2개 테스트 파일, 5개 테스트 모두 통과했다.
