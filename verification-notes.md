# Route 지도 안내 기능 검증 기록

## 2026-08-19

- 412×900 모바일 뷰포트에서 확장된 장소 시트의 영업시간, 사진 미리보기, 상세 진입 버튼을 확인했다.
- 412×900 모바일 뷰포트에서 최근 검색어 패널의 노출과 검색 화면 배치를 확인했다.
- 최신 모바일 미리보기에서 기본 Google 지도 타일, 장소 마커, 카테고리 필터, 하단 장소 시트가 함께 정상 렌더링되는 것을 다시 확인했다.
- 실제 사용자 데이터 기반 친구 탭에서 하드코딩된 여행자·코스 대신 팔로잉·추천 여행자·최근 공개 코스별 빈 상태 안내와 사용자 검색 진입 버튼이 표시되는 것을 확인했다.
- 공개 범위·현재 위치 중심 탐색 개선 후 최신 모바일 미리보기에서 Google 지도 타일과 장소 마커·현재 위치 제어·카테고리 칩·하단 장소 시트가 정상 로드되는 것을 확인했다.
- 실제 모바일 코스 작성 1단계에서 비공개와 전체 공개를 나란히 선택할 수 있고, 전체 공개에는 다른 Route 사용자가 링크로 열 수 있다는 안내가 함께 표시되는 것을 확인했다.
- 공개 범위 선택은 4단계 코스 작성의 첫 화면에서 제목·여행 기간·상태와 함께 표시되어 공개 여부를 저장 전에 명확히 결정할 수 있다.
- 테스트 위치 좌표(서울 시청 인근)를 제공한 모바일 브라우저에서 현재 위치 버튼을 누르자 **현재 위치로 이동했습니다** 안내가 표시되고, 지도 중심이 서울 시청 인근으로 이동했으며 성수 장소의 거리도 약 7.3km로 다시 계산되는 것을 확인했다.
- 같은 위치에서 **맛집** 카테고리를 선택하자 Google Places 주변 검색 결과 12곳이 지도 마커와 하단 목록에 함께 표시됐다. 예를 들어 `Din Tai Fung Myeongdong`은 현재 위치 기준 약 580m로 계산되어, 카테고리 탐색이 기본 성수 데이터가 아닌 현재 위치 기준으로 동작함을 확인했다.

## 2026-08-20 — 공유 미리보기·지역 선택·전체 지도 탐색

- 위치 권한을 거부한 실제 모바일 미리보기에서 안내 대화상자에 **지역 선택**과 **다시 시도**가 함께 표시됐다. 지역 선택을 누르면 지도 위에 지역 검색 시트가 열리고, 현재 위치 권한 없이도 장소명·지역명을 입력해 탐색 기준을 바꿀 수 있는 안내가 노출됐다.
- 개발 서버 갱신 뒤 지도 화면을 다시 열어도 위치 권한 거부 안내와 지역 선택 대체 동선이 유지되어, 권한 상태와 무관하게 사용자가 탐색을 계속할 수 있음을 확인했다.
- 안내를 닫으면 지도·카테고리·주변 장소 시트가 즉시 다시 활성화되어, 권한 안내가 일반 지도 탐색을 막지 않는 것을 확인했다.
- 지도 빈 영역을 누르자 장소 정보 시트·검색·카테고리 영역이 숨겨지고, 지도만 전체 화면으로 확장됐다. 우측의 **전체 지도 닫기** 제어도 함께 표시되어 일반 지도 화면으로 돌아갈 수 있었다.
- 실제 브라우저에서 현재 위치 버튼을 누른 뒤 geolocation 권한 거부 응답을 재현했고, **현재 위치 권한이 필요합니다** 안내 팝업과 **다시 시도**·**나중에** 버튼이 노출되는 것을 확인했다.
- 390×844 모바일 최종 미리보기에서 기본 Google 지도 타일, 일반 장소 핀과 구분되는 파란색 현재 위치 핀, 검색 바·카테고리 칩·현재 위치/지역 선택 제어, 하단 장소 시트가 겹침 없이 함께 표시되는 것을 확인했다.
- 공개 코스 공유 경로는 OG 제목·요약·대형 미리보기 이미지 URL을 반환하고, 1200×630 SVG 미리보기에는 코스명과 대표 장소가 표시되는 서버 테스트로 검증했다. 비공개 코스는 동일 경로에서 404로 처리된다.
- 위치 권한 거부 안내의 **지역 선택** 동작, 지도 빈 영역의 전체 지도 전환·닫기, 공개 코스의 OG 공유 URL 복사 흐름을 UI 회귀 테스트로 확인했다.
- `pnpm check && pnpm test` 결과 TypeScript 검사와 Vitest **68개**가 모두 통과했다.

## 2026-08-20 — 공유 대표 사진·전체 지도 클러스터링·최근 탐색 지역

- 공개 코스의 SVG 미리보기는 첫 번째 장소에 실제 사진 URL이 있으면 해당 이미지를 대표 사진으로 사용하고, 사진이 없을 때만 코스 커버 또는 일러스트 fallback을 사용한다. 공개 여부 검증도 기존과 동일하게 유지했다.
- 전체 화면 지도에서는 줌 수준에 따라 가까운 핀을 보라색 수량 배지형 클러스터로 묶는다. 클러스터를 누르면 해당 중심으로 이동하며 두 단계 확대되어 개별 장소 핀을 다시 선택할 수 있다.
- 지역 선택 시트에는 최근 탐색 지역을 최대 5개까지 브라우저 저장소에 보존해 다시 선택할 수 있게 했고, 각 항목 및 전체 기록의 삭제 제어를 추가했다.
- 390×844 모바일 지도 미리보기에서 검색 바·카테고리·현재 위치/지역 선택 제어·장소 시트가 기존 레이아웃을 유지하는 것을 확인했다. TypeScript 검사 및 Vitest **71개**가 모두 통과했다.

## 2026-08-20 — 출시 준비 검증

- 사용자에게 노출되던 임시 장소의 평점·리뷰 수, 임시 공개 코스·작성자·좋아요 수를 정리했다. 평점·리뷰는 Google Places가 반환한 실제 값이 있는 경우에만 표시하고, 공개 코스는 실제 공개 데이터만 사용한다.
- 장소 행을 장소 열기·길찾기·저장 버튼으로 분리해 중첩 버튼 구조를 제거했다. 마이페이지에는 **데이터·공개 범위 안내**를 추가하고, 첫 로그인 화면에는 장소 저장·코스 제작·위치 및 공개 범위 선택권을 요약했다.
- 390×844 모바일 지도에서 검색 바·카테고리·현재 위치 제어·장소 시트가 겹치지 않고 표시되는 것을 확인했다.
- 최종 `pnpm check && pnpm test && pnpm build` 결과 TypeScript 검사, Vitest **72개**, 프로덕션 번들 생성이 모두 통과했다. 별도 프로덕션 서버 스모크 테스트에서 `/`는 200, 공개되지 않은 공유 코스 경로는 404를 반환했다.
- 출시 전 기술 검증·사용자 확인 항목은 `launch-readiness.md`에 기록했다. 프로덕션 빌드는 초기 JavaScript 청크 크기 경고를 출력하지만, 빌드·정적 응답은 정상 완료됐다.

## 2026-08-20 — 소셜 공유·지도 첫 사용 튜토리얼

- 공개 코스 공유 시트에 트위터와 페이스북 공유 버튼을 추가했다. 두 버튼은 공개 코스의 `/share/course/:id` 링크만 전달하므로 기존 OG 제목·요약·대표 사진 미리보기를 함께 활용한다. 비공개 코스에서는 공개 전환 안내를 표시한다.
- 첫 지도 진입 시 검색·카테고리 탐색, 장소 핀의 빠른 작업, 전체 지도 숫자 클러스터의 장소 목록을 세 단계로 안내한다. 완료·건너뛰기 상태는 `localStorage`에 저장하며, 마이페이지의 **지도 사용 가이드 다시 보기**에서 언제든 재시작할 수 있다.
- 390×844 모바일 화면에서 튜토리얼 카드가 지도 위에 선명하게 표시되고, 위치 권한 안내와 겹치지 않는 것을 확인했다. TypeScript 검사와 Vitest **74개**가 모두 통과했다.
- 넓은 화면에서도 튜토리얼 오버레이와 검색 바 강조 영역이 Route의 412px 앱 프레임 안에만 표시되도록 위치 기준을 보정했고, 1280×720 미리보기에서 프레임 바깥으로 확장되지 않는 것을 확인했다.

## 2026-08-20 — 지도 튜토리얼 모션

- 튜토리얼 카드는 320ms의 짧은 위쪽 진입 모션으로 표시되고, 각 단계 콘텐츠는 좌우 전환으로 이어진다. 검색 바 강조 영역과 아이콘에는 과하지 않은 반복 움직임을 적용해 다음 행동에 시선을 유도했다.
- `prefers-reduced-motion: reduce` 환경에서는 반복 애니메이션과 전환을 비활성화한다. 390×844 모바일 화면에서 카드·진행 막대·강조 영역이 레이아웃을 가리지 않는 것을 확인했고, TypeScript 검사와 Vitest **74개**가 모두 통과했다.

## 2026-08-21 — 튜토리얼 지도 강조·예시 코스·현재 위치 추천

- 튜토리얼의 장소 핀 단계와 클러스터 단계에서는 해당 지도 좌표에 별도 보라색 강조 마커를 올린다. 단계 전환 시 이전 강조 마커를 정리하고 새 대상 중심으로 지도를 이동하므로, 사용자가 설명과 실제 지도 요소를 함께 확인할 수 있다.
- 각 튜토리얼 카드에는 **성수 오후 산책** 예시 코스를 표시해 `성수 식당 → 오븐 성수 → 서울숲`의 장소 저장·코스 추가·시간순 일정 완성 흐름을 짧게 보여준다.
- 현재 위치가 있으면 카테고리별 Google Places 주변 검색은 사용자 위치를 우선 사용하고, 결과를 거리순으로 정렬한다. 장소 시트에도 현재 위치 주변 추천 상태를 명시한다. 위치 권한이 없을 때만 선택 지역을 대체 기준으로 사용한다.
- 390×844 모바일 지도에서 튜토리얼 카드·예시 코스·검색 강조가 겹치지 않고 표시되는 것을 확인했으며, TypeScript 검사와 Vitest **74개**가 모두 통과했다.

## 2026-08-21 — 현재 위치 자동 주변 장소 추천

- 현재 위치가 허용되면 좌표 이동과 파란 현재 위치 핀 표시 뒤, 하단 시트가 Google Places `nearbySearch`로 반경 5km의 실제 주변 장소를 자동으로 불러온다. 기본 자동 추천은 전체 카테고리로 시작하며, 이후 맛집·카페·관광지·숙소 전환도 같은 현재 위치를 기준으로 실행된다.
- 자동 추천 결과는 현재 위치와의 직선 거리순으로 정렬되고, 시트 제목에 **현재 위치 주변 전체 추천** 또는 해당 카테고리 추천 상태를 표시한다. 권한 거부 시에는 기존 지역 직접 선택 안내를 유지한다.
- Google Maps·geolocation 목을 사용해 자동 추천 결과가 하단 시트에 거리순으로 표시되는 UI 회귀 테스트를 추가했다. TypeScript 검사와 Vitest **75개**가 모두 통과했다.

## 2026-08-21 — 여행지·음식점·카페 중심 주변 추천

- 현재 위치 자동 추천은 더 이상 모든 장소 유형을 한 번에 가져오지 않고, Google Places의 `tourist_attraction`·`restaurant`·`cafe` 결과를 병합해 **여행지·음식점·카페** 중심으로 표시한다. 중복 장소를 제거한 뒤 현재 위치와의 거리순으로 정렬한다.
- 하단 장소 시트에 **추천·여행지·음식점·카페·숙소** 카테고리 선택기를 추가했다. 선택 즉시 같은 현재 위치 또는 권한 거부 시 사용자가 고른 지역 기준으로 결과를 다시 불러오며, 선택 상태와 추천 제목을 표시한다.
- 통합 추천·여행지 전환·거리순 하단 시트·권한 거부 흐름을 포함한 UI 및 소스 회귀 테스트를 확인했고, TypeScript 검사와 Vitest **75개**가 모두 통과했다.

## 2026-08-21 — 현재 영업 중 필터·평점·영업시간

- 하단 추천 시트의 **현재 영업 중** 버튼은 실제 Places `opening_hours.isOpen()` 결과가 확인된 장소만 남기며, 선택 여부는 접근성 상태로도 전달한다. 영업 상태를 확인할 수 없는 장소는 이 필터에서 제외해 결과의 신뢰도를 유지한다.
- 각 추천 장소 행에는 실제 평점과 리뷰 수, 영업 중·종료 상태, Google Places 상세 응답의 오늘 영업시간, 주소와 거리를 함께 표시한다. 평점·리뷰 데이터가 없는 장소에는 해당 값을 임의로 만들지 않는다.
- Google Places 상세 응답 목을 사용해 평점·리뷰 수·영업시간 표시와 토글 상태를 검증했다. 390×844 지도·튜토리얼 화면이 기존 레이아웃을 유지하는 것을 확인했고, TypeScript 검사와 Vitest **75개**가 모두 통과했다.

## 2026-08-21 — 영업시간 조건부 표시

- Google Places의 오늘 영업시간(`weekday_text`)이 실제로 제공된 장소에만 하단 추천 행·선택 장소 시트·장소 상세의 영업시간 영역을 표시하도록 조정했다. 단순 영업 상태와 기존 대체 문구는 영업시간으로 표시하지 않는다.
- 영업시간이 없는 기본 장소와 주변 추천 결과에서는 해당 영역이 렌더링되지 않고, 시간 정보가 있는 추천 결과에는 오늘 영업시간이 표시되는 UI 회귀 테스트를 추가했다. TypeScript 검사와 Vitest **75개**가 통과했고, 390×844 모바일 지도 화면의 레이아웃도 확인했다.

## 2026-08-21 — 장소 상세 정보 계층·한국어 표기

- 장소 상세를 **지도 위치 → 사진 → 장소 소개·기본 정보 → 예약·문의 → 저장·코스 추가**의 독립 영역으로 분리했다. 지도 아래에는 한국어 위치 요약을 두고, 사진에는 별도 제목·장수를 표기해 두 콘텐츠가 하나의 시각 덩어리처럼 보이지 않게 했다.
- 실제 상호명은 원문 그대로 유지하고, 주소·영업시간·전화번호·예약·지도 이동 안내는 한국어 중심 문구로 정리했다. 장소 상세의 지도·사진 영역 접근성, 한국어 링크, 갤러리 진입을 포함한 TypeScript 검사과 Vitest **76개** 테스트가 통과했으며, 390×844 모바일 지도 로드 상태도 확인했다.

## 2026-08-21 — 현재 위치 추천 초기화 오류 확인

- `categoryFromPlaceTypes`를 컴포넌트 내부 상수에서 모듈 수준 함수 선언으로 옮겨, Google Places 비동기 주변 검색 콜백이 초기화 전 참조를 일으키지 않게 했다.
- 수정 후 실제 지도 페이지를 새로 열어 장소 핀·하단 시트·추천 카테고리 제어가 렌더링되는 것을 확인했다. 기존 브라우저 콘솔의 ReferenceError는 수정 전 시각의 기록이며, 최신 로드에서는 같은 오류가 재현되지 않았다.

## 2026-08-20 — 클러스터 장소 목록·공유 사진 설정·최근 지역 즐겨찾기

- 전체 화면 지도에서 수량 배지형 클러스터를 선택하면, 포함된 장소의 사진·이름·카테고리·주소를 간단한 바텀시트 목록으로 보여준다. 각 행을 누르면 해당 장소 프리뷰로 이동하며, 별도 제어로 클러스터 영역을 확대할 수 있다.
- 코스 생성·수정 화면에 장소 사진을 선택하는 **공유 미리보기 대표 사진** 영역을 추가했다. 선택값은 `courses.shareImageUrl`에 저장되며, 공개 코스 OG SVG는 이 값을 장소 기본 사진보다 우선 사용한다. 데이터베이스 컬럼이 실제로 생성된 것을 확인했다.
- 최근 탐색 지역은 하트로 즐겨찾기 고정·해제할 수 있고, 즐겨찾기 지역은 브라우저 저장소에서 유지되며 목록 최상단에 정렬된다.
- 390×844 모바일 미리보기에서 현재 위치 권한 안내·지도·검색·하단 장소 시트가 겹침 없이 유지되는 것을 확인했다. TypeScript 검사와 Vitest **71개**가 모두 통과했다.

## 2026-08-19 — Google Places 사진 및 지도 관리 기능

실제 브라우저에서 `성수 카페`를 검색해 Google Places 결과의 대표 사진이 선택 장소 프리뷰와 결과 목록에 표시되는 것을 확인했다. 각 검색 결과는 Google Places가 반환한 사진 참조를 사용하며, 제공되지 않는 경우에만 기존 기본 이미지를 사용한다.

선택한 Google Places 결과의 장소 상세 화면에서도 반환된 실제 사진이 상단 사진 영역에 적용되는 것을 확인했다. 검색 화면에서는 `성수 카페` 최근 검색어의 개별 삭제 버튼을 눌러 목록이 즉시 사라지는 것을 확인했다.

전체 삭제 동선 검증을 위해 브라우저 저장소에 두 개의 최근 검색어를 준비했다. 또한 실제 지도에서 `오븐 성수`를 저장한 뒤, 저장 장소 상태를 다시 불러와 빠른 코스 추가 버튼 노출과 중복 방지를 확인하는 흐름을 진행 중이다.

저장 후 새로고침한 실제 지도에서 `오븐 성수`를 선택하자 **현재 코스에 담김** 버튼이 표시되어 저장 장소 판별과 중복 방지가 반영된 것을 확인했다. 검색 화면에서는 `성수 맛집`과 `서울숲` 최근 검색어에 대해 **전체 삭제**를 실행했고, 최근 검색어 카드가 즉시 사라졌다.

`현재 코스에 담김` 버튼을 다시 선택했을 때 **이미 현재 여행 코스에 담긴 장소입니다** 안내가 표시되어 중복 추가가 차단되는 것을 실제 모바일 화면에서 확인했다.

빠른 코스 추가 상태를 실제로 확인하는 과정에서 `오븐 성수`를 내 장소에 임시 저장했다. 검증 후 저장 토글을 다시 실행하고 새로고침한 뒤 빠른 추가 버튼이 사라진 것을 확인하여, 이 검증용 사용자 데이터는 제거했다.

## 네이버 연결 참고

- 네이버 클라우드 공식 문서는 네이버 지도 앱의 `nmap://` URL Scheme과 장소·통합 검색 연결 방식을 안내한다. Route의 웹 환경에서는 사용자가 설치 여부와 관계없이 접근할 수 있도록 네이버 지도 웹 검색 링크를 새 창으로 열고, 장소명과 주소를 검색어로 전달한다.
- 출처: https://guide.ncloud-docs.com/docs/maps-url-scheme

## 지도 탐색 및 코스 선택 검증

- 실제 모바일 지도 화면에서 거리순·영업 중 필터가 하단 장소 시트에 노출되고, 거리 표기가 기본 지도 중심 기준으로 갱신되는 것을 확인했다.
- 장소 핀을 선택하면 장소 프리뷰의 `코스 선택` 버튼이 표시되고, 누르면 새 코스 만들기와 기존 내 여행 코스 목록을 함께 제공하는 바텀 모달이 노출되는 것을 확인했다.
- 실제 장소 상세 화면에서 사진 클릭용 확대 버튼과 `네이버에서 예약·문의` 링크가 표시되며, 링크가 네이버 지도 장소 검색 URL로 연결되는 것을 확인했다.
- 홈 화면에서 진행 중인 코스 카드가 다음 장소·시간·장소 수를 표시하고, `코스 보기`를 누르면 전체 일정·다음 장소·지도 이어가기 동선이 있는 전용 화면으로 진입하는 것을 확인했다.
- 장소 검색 화면에서 거리순·영업 중 필터가 검색 결과와 함께 유지되는 것을 확인했고, 실제 Google Places 결과의 다중 사진 갤러리를 추가 검증할 수 있도록 검색 흐름을 준비했다.
- `성수 카페` 실제 Google Places 검색에서 장소별 대표 사진·실제 평점·주소·거리와 다수의 결과가 반환되는 것을 확인했다.
- 실제 장소 상세 진입 시 Google Places Details를 추가 조회하여 최대 다섯 장의 고해상도 사진·영업 상태·전화번호를 갤러리 데이터로 보강하도록 구현했고, 관련 소스 회귀 테스트와 전체 24개 Vitest 테스트를 통과했다.
- 실제 `Cafe Onion Seongsu` 상세에서 Google Places Details 사진 5장이 로드되고, 세 장의 미리보기와 `+2` 표기가 노출되는 것을 확인했다. 두 번째 사진을 전체 화면으로 열어 `2 / 5` 상태를 확인한 뒤, 다음 사진 동작으로 `3 / 5`와 이미지 전환이 정상 반영되는 것을 모바일 화면에서 확인했다.
- 같은 갤러리에서 이전 사진 동작으로 `3 / 5`에서 `2 / 5`로 되돌아가는 것을 확인했고, 닫기 버튼을 눌러 다중 사진이 유지된 장소 상세 화면으로 복귀하는 것을 확인했다. UI 회귀 테스트에도 다중 사진의 이전·다음·닫기 단계를 추가했으며 전체 24개 테스트가 통과했다.

## 2026-08-19 — 네이버 예약 연결 설계 참고

- 네이버 클라우드의 공식 지도 앱 URL Scheme 문서는 `nmap://search?query=...&appname=...` 형태의 통합 검색을 제공한다고 안내한다. 웹에서 네이버 지도 앱 미설치 시에도 예약·문의 탐색을 끊지 않기 위해, Route는 우선 실제 네이버 예약·플레이스 URL을 사용하고 그렇지 않은 경우 장소명·주소를 포함한 네이버 웹 검색 결과 URL로 연결한다. 출처: https://guide.ncloud-docs.com/docs/maps-url-scheme

## 2026-08-19 — 코스 생명주기와 일정 경고 검증

- 실제 모바일 브라우저의 내 코스 편집 화면에서 시작일·종료일 입력, 예정·진행 중·완료 상태 선택, 그리고 방문 시간 기반의 일정 경고 카드가 함께 렌더링되는 것을 확인했다. `성수 식당`의 10:00 방문 시간은 `11:30 - 22:00` 영업시간을 벗어난다는 경고로 표시됐다.
- 실제 모바일 장소 상세에서 **네이버에서 예약 찾기** 링크가 장소명·주소·네이버 예약 검색어를 포함한 네이버 검색 결과 URL로, **네이버 지도에서 장소만 검색하기** 링크가 동일 장소의 네이버 지도 검색 URL로 표시되는 것을 확인했다. 홈·진행 중 코스의 기간·상태와 코스 상세 경고는 UI·소스 회귀 테스트를 보강했으며, 전체 27개 테스트가 통과했다.
- 저장 장소와 코스 항목에 `hours` 필드를 추가해 실제 Google Place 또는 사용자가 저장한 영업시간을 코스 생성·수정·기존 코스 추가 흐름에 보존하도록 보강했다. 카탈로그 fallback이 아닌 저장된 `12:00 - 20:00` 영업시간으로 종료 경고가 발생하는 단위 테스트와 시간 겹침 테스트를 추가했고, 전체 29개 테스트가 통과했다.
- 실제 모바일 공개 코스 상세에서 **Day 1**·**Day 2** 탭, 전체 예상 소요시간 `4시간 30분`, 선택된 Day 1 소요시간 `2시간 30분`, 해당 일차의 장소 타임라인이 함께 표시되는 것을 확인했다.
- Day 2 탭을 선택하자 선택 일차 소요시간이 `2시간`으로 바뀌고, `오설록 티 뮤지엄`과 `애월 해안도로`만 시간순 타임라인에 표시되는 것을 실제 모바일 화면에서 확인했다.

## 2026-08-19 — 일차별 일정·체류시간·드래그 재정렬 최종 검증

- 실제 모바일 브라우저의 공개 코스 상세에서 Day 1·Day 2 탭을 전환할 때 선택 일차의 타임라인과 일차별 소요시간이 함께 바뀌는 것을 확인했다.
- 코스 생성 2단계의 4개 장소 목록에서 첫 번째 `성수 식당`의 드래그 핸들을 잡아 세 번째 위치로 이동했다. 이동 후 목록이 `오븐 성수 → 성수동 스테이크 → 성수 식당 → 서울숲` 순으로 다시 렌더링되어 모바일 포인터 기반 재정렬이 정상 동작함을 확인했다.
- 코스 수정 화면도 동일한 포인터 재정렬 경로를 지원하도록 보완했으며, 생성·수정 화면의 모바일 드래그 재정렬을 포함한 UI 회귀 테스트와 전체 검증에서 TypeScript 검사 및 Vitest 33개 테스트가 통과했다.

## 2026-08-19 — 이동시간·일차별 지도 경로·공유 검증

- 실제 412×900 모바일 화면에서 공개 코스 공유 URL `?course=c1`로 직접 진입하자 **제주 2박 3일 힐링 코스** 상세가 즉시 열렸다. Day 1 타임라인에는 `협재 해수욕장 → 애월 카페거리` 사이의 **약 5분 이동 · 80m**가 장소 카드 사이에 표시됐고, 지도에는 Day 1 경로만 강조됐다.
- Day 탭 전환 시 선택 일차 장소·지도 경로·구간 이동시간이 함께 바뀌는 흐름을 확인했다. 동선 경고 계산은 우회 장소가 직선 대비 55% 이상, 600m 이상 추가 이동을 만들 때 안내하도록 단위 테스트로 검증했다.
- 공유 시트에서 링크 복사와 이미지 저장 선택지가 노출되는 것을 확인했다. 이미지 저장 핸들러는 실제 모바일 브라우저에서 `Route_제주 2박 3일 힐링 코스.png` 파일명과 `data:image/png;base64,` PNG 데이터 URL(158,022자)을 생성해 다운로드 요청을 만드는 것을 계측으로 확인했다. 관리형 미리보기 브라우저의 다운로드 목록은 비어 있었지만, 내보내기 함수 호출과 PNG 데이터 생성은 확인됐다.
- TypeScript 검사와 이동시간·딥링크·PNG 내보내기·우회 경고 테스트를 포함한 Vitest **40개**가 통과했다.
- 실제 모바일 공개 코스 상세에서 Day 2를 선택하자 `오설록 티 뮤지엄 → 애월 해안도로`의 **1.8km** 구간에 **동선을 한 번 확인해보세요** 경고 카드가 표시됐다. 카드에는 이동 수단 또는 방문 순서를 확인하라는 안내와 해당 Day의 지도 경로·구간 이동시간이 함께 노출됐다.

## 2026-08-19 — 재정렬 이동시간 및 경로 추천 검증

- 실제 모바일 코스 생성 2단계에서 장소 목록을 `성수 식당 → 서울숲 → 오븐 성수 → 성수동 스테이크`로 포인터 드래그하자, 지도 아래의 현재 순서 예상 이동·구간별 이동시간 카드가 즉시 갱신되고 **추천 순서 적용** 버튼이 표시됐다.
- 같은 화면에서 **추천 순서 적용**을 누르자 목록이 `성수 식당 → 오븐 성수 → 성수동 스테이크 → 서울숲`으로 재정렬됐으며, 카드가 **현재 순서가 추천 동선과 같습니다** 상태로 전환됐다.
- 경로 추천은 각 Day의 첫 장소를 출발점으로 고정하고, 8개 이하 장소는 총 이동거리 최소 조합을 계산한다. 9개 이상은 안전한 근접 탐색 방식으로 처리한다. TypeScript 검사와 Vitest **43개**가 통과했다.

## 2026-08-19 — 전체 장소 이동시간 및 수정 코스 추천 검증

- 코스의 이동시간·거리·추천 순서는 첫 8개가 아닌 **전체 장소**를 기준으로 계산하도록 보완했다. 10개 장소 입력 시 9개 인접 구간이 모두 생성되는 단위 테스트를 추가했다.
- 실제 모바일 코스 수정 화면에서 `성수 식당 → 서울숲 → 오븐 성수 → 성수동 스테이크` 순서로 마지막 장소를 두 번째 위치로 드래그하자, 현재 순서 예상 이동 요약과 지도 경로가 즉시 갱신되고 **추천 순서 적용** 버튼이 노출됐다.
- 추천 순서 적용 후 목록은 `성수 식당 → 오븐 성수 → 성수동 스테이크 → 서울숲`으로 정렬됐고, 지도 경로·구간 이동시간과 **현재 순서가 추천 동선과 같습니다** 안내가 함께 갱신됐다. 생성·수정 화면의 UI 회귀 테스트와 전체 검증에서 TypeScript 검사 및 Vitest **44개**가 통과했다.

## 2026-08-19 — 이동 수단 및 장소 길찾기 검증

- 실제 모바일 지도에서 `성수 식당`을 선택한 뒤 장소 상세의 **길찾기** 버튼으로 진입했다. 출발지·도착지, 자동차·대중교통·도보 수단 선택, 지도 경로와 외부 네비 앱 연결 카드가 한 화면에 표시됐다.
- 대중교통을 선택하자 선택 상태와 Route 예상 이동 요약이 즉시 갱신됐고, Google Maps 링크도 `travelmode=transit`을 포함한 대중교통 길찾기 URL로 변경됐다. 네이버지도는 현재 위치 출발 `nmap://navigation` URL Scheme, 카카오맵은 선택 목적지 링크로 함께 제공된다.
- TypeScript 검사와 UI 회귀 테스트를 포함한 Vitest **45개**가 통과했다.

## 2026-08-19 — 수단별 추천 및 검색 결과 직접 길찾기 최종 검증

- 자동차와 대중교통은 도시 격자형 도로 거리, 도보는 직선 거리와 수단별 속도를 사용해 구간 시간·전체 이동시간·추천 순서를 계산하도록 보완했다. 대각선에 있는 장소와 직선상 장소를 함께 둔 단위 테스트에서 도보는 대각선 장소를, 자동차는 직선 장소를 먼저 추천하는 것을 확인했다.
- 실제 모바일 지도 화면의 `성수 식당` 검색 결과에서 행 전체를 열지 않고 **길찾기** 아이콘만 눌렀다. 장소 상세를 거치지 않고 Route 길찾기 화면이 즉시 열리며 도착지 이름·주소와 현재 위치 기반 출발지 상태가 노출되는 것을 확인했다.
- 같은 화면에서 **대중교통**을 선택하자 이동 요약이 대중교통으로 전환되고 Google Maps 링크가 `travelmode=transit`을 포함한 URL로 갱신됐다. 네이버지도 `nmap://navigation` 및 카카오맵 목적지 링크도 함께 유지됐다.
- `pnpm check && pnpm test` 결과 TypeScript 검사와 Vitest **49개**가 모두 통과했다.

## 2026-08-19 — 지도 프리뷰 및 수단별 추천 최종 추가 검증

- 실제 모바일 지도에서 `오븐 성수` 마커를 선택하자 프리뷰 카드에 **저장 · 길찾기 · 코스 선택** 액션이 표시됐다. 이 카드의 **길찾기**를 누르자 장소 상세를 거치지 않고 `오븐 성수`를 도착지로 하는 Route 길찾기 화면으로 전환됐다.
- 코스 생성 2단계의 기본 성수 4개 장소에서 자동차를 선택했을 때, 현재 이동 요약은 **약 15분 · 2.1km**로 표시됐고 추천 순서는 `성수 식당 → 성수동 스테이크 → 오븐 성수 → 서울숲`으로 계산됐다. 현재 순서 대비 **10m, 약 0분** 절감 안내와 추천 적용 버튼도 노출됐다.
- 같은 코스에서 도보를 선택하자 이동 요약은 **약 29분 · 2.1km**로 갱신됐고, `성수 식당 → 오븐 성수 → 성수동 스테이크 → 서울숲` 현재 순서가 추천 동선과 같다는 상태로 전환됐다. 즉, 수단 전환에 따라 현재 이동시간과 추천 순서·추천 적용 상태가 실제 모바일 UI에서 바뀌는 것을 확인했다.
- 표시용 반올림 최소 시간과 분리한 정밀 수단별 비용으로 추천을 계산하도록 보완했다. `pnpm check && pnpm test` 결과 TypeScript 검사와 Vitest **51개**가 모두 통과했다.

## 2026-08-19 — 출발지·즐겨찾기·공유 통합 검증

- 실제 모바일 검색 결과에서 `성수 식당`의 길찾기 버튼으로 진입한 뒤 **출발지 변경**을 열었다. `오븐 성수`를 직접 입력해 적용하자 출발지 표기, 지도 출발점, 예상 거리(10m → 80m), Google Maps `origin`, 네이버지도 `slat`·`slng`·`sname`이 모두 `오븐 성수` 기준으로 갱신됐다.
- 같은 출발지를 **현재 위치 저장**으로 등록하자 `오븐 성수 · 서울 성동구 연무장길 7` 즐겨찾기 행과 삭제 액션이 나타났고, 추가 성공 토스트가 표시됐다. 즐겨찾기는 브라우저 저장소에 유지되도록 구현했다.
- 길찾기 헤더와 이동 요약의 공유 아이콘을 누르자 **출발지·도착지·이동 수단·시간·거리**가 요약된 공유 시트가 열렸다. 이 시트에서 URL을 복사하거나 기기 기본 공유 시트를 열어 카카오톡을 선택할 수 있다. 공유 URL은 Route가 수신하면 목적지·출발지·이동 수단을 복원해 길찾기 화면을 연다.
- 실제 모바일 코스 생성 2단계에서 추천 카드에 **현재 순서**와 **추천 순서**의 예상 시간·거리(`약 15분 · 2.1km` 등)가 좌우 비교로 함께 표시되고, 절감 안내·추천 순서 적용·공유 버튼이 노출되는 것을 확인했다. 추천 동선 공유 URL은 수신 시 동일한 장소 순서와 이동 수단으로 코스 생성 2단계를 연다.
- 출발지 지정·즐겨찾기·길찾기 공유·추천 비교를 포함한 TypeScript 검사와 Vitest **54개**가 모두 통과했다. 기기 공유 시트는 외부 전송이 발생할 수 있어 실제 전송은 실행하지 않고, 카카오톡 선택 버튼과 링크 복사 버튼의 노출·핸들러 경로를 검증했다.

## 2026-08-19 — 네이버 내비 중심 길찾기 단순화

- 자체 길찾기·구간별 이동 시간·복수 지도 앱 선택을 제거했다. Route는 출발지와 도착지를 점선으로 표시한 **직선 거리 개요 지도**와 거리 카드만 제공한다.
- 실제 모바일에서 `성수 식당` 검색 결과의 길찾기 버튼을 눌러 확인했다. `성수동 기준 위치 → 성수 식당` 정보, 출발/도착 마커, **직선 거리 10m**, 그리고 `네이버 내비 열기` 기본 버튼이 순서대로 표시됐다.
- 네이버 내비 링크에는 목적지와 출발지 좌표·이름을 전달하는 `nmap://navigation` URL이 포함됐고, 앱을 사용하지 않는 경우를 위한 **네이버지도 사이트에서 목적지 보기** 링크도 함께 노출됐다. 외부 내비 실행은 실제 기기 전환을 유발할 수 있어 URL 생성과 UI 노출까지만 확인했다.
- TypeScript 검사 및 Vitest **54개**가 모두 통과했다.

## 2026-08-19 — 장소 상세 네이버 내비 출발 확인 및 예상 시간

- 실제 모바일 장소 상세의 하단에 초록색 **네이버 내비** 고정 버튼이 표시되는 것을 확인했다. 사용자는 예약·저장·코스 선택 동선을 방해받지 않고 화면 하단에서 바로 길안내 준비를 시작할 수 있다.
- 고정 버튼을 누르면 외부 앱으로 바로 이동하지 않고, `성수동 기준 위치 → 성수 식당`의 출발·도착 정보, 목적지 주소, **직선 거리 10m**를 다시 보여주는 출발 확인 바텀시트가 열린다.
- 시트에는 자동차·대중교통·도보별 예상 시간을 함께 표시했다. 대중교통을 선택하자 하단 버튼 문구가 **대중교통 기준 선택됨 · 약 5분**으로 갱신되는 것을 실제 모바일 화면에서 확인했다.
- `네이버 내비로 출발` 링크에는 출발지·목적지 좌표와 이름을 담은 `nmap://navigation` URL이 유지된다. 실제 외부 앱 실행은 기기 전환을 발생시키므로 URL·UI 노출까지만 확인했다. TypeScript 검사 및 Vitest **55개**가 모두 통과했다.

## 2026-08-19 — 최근 목적지·지도 출발지 선택 및 오류 수정

- 네이버 내비 출발을 선택하면 목적지를 브라우저 저장소의 최근 목적지 목록에 최대 6개까지 자동 저장하고, 중복 목적지는 가장 최근 순서로 올리도록 구현했다. 다음 출발 확인 시트에서 최근 목적지를 다시 눌러 도착지로 바꿀 수 있다.
- 실제 모바일 길찾기에서 기본 출발지는 `성수동 기준 위치`로 표시됐다. **지도에서 출발지 선택**을 연 뒤 지도 내부를 누르자 역지오코딩 주소가 출발지로 적용되고, 직선 거리와 네이버 내비 URL의 `slat`·`slng` 좌표가 함께 갱신되는 것을 확인했다.
- Google Places의 폐기된 `opening_hours.open_now` 접근을 `isOpen()` 기반 공용 헬퍼로 교체해 관련 콘솔 경고 발생 경로를 제거했다. 새 기능을 포함한 TypeScript 검사와 Vitest **56개**가 모두 통과했다. 실제 네이버 내비 외부 실행은 기기 전환을 유발하므로 자동 저장은 회귀 테스트로 검증했다.

## 2026-08-19 — 네이버 내비 전환 안정화 및 단순 지도 UI

- 네이버 공식 모바일 웹 안내에 맞춰 Android에서는 `intent://navigation` URL로 네이버 지도 앱을 실행하고 앱이 없으면 설치 경로를 제공하도록 변경했다. iOS는 `nmap://navigation`을 실행한 뒤 앱 전환이 되지 않으면 네이버지도 웹으로 대체한다. 데스크톱·미지원 브라우저는 즉시 네이버지도 웹 검색을 새 탭으로 연다.
- 실제 모바일 미리보기 환경에서 `네이버 내비 열기 → 네이버 내비로 출발`을 누르자 `https://map.naver.com/p/search/성수 식당…`으로 전환되는 것을 확인했다. 미리보기에는 네이버 앱이 없으므로 웹 대체가 작동한 결과이며, 실제 Android 기기에서는 Intent URL이 우선 실행된다.
- 지도는 모든 도로·상업 POI·텍스트 라벨을 숨기고, 연한 지형·물·공원·행정 경계와 Route 장소 마커만 보이게 재구성했다. 실제 모바일 지도에서 검색 바, 카테고리, 현재 위치, 4개 장소 마커, 하단 장소 시트가 유지되는 것을 확인했다.
- TypeScript 검사와 Vitest **56개**가 모두 통과했다.

## 2026-08-19 — 네이버 내비 미설치 안내 및 지도 시안 복원

- 길찾기 화면의 `네이버 내비 열기`도 출발 확인 시트를 거치도록 통일했다. 확인 시트에서 **네이버 내비 앱이 설치되어 있지 않나요?**를 누르면 별도 안내 바텀시트가 열린다.
- 안내 시트는 네이버지도 앱 설치, 네이버지도 웹 길찾기, 앱에서 다시 확인의 세 가지 선택지를 제공한다. Android/데스크톱에서는 Google Play, iOS에서는 Apple App Store 설치 링크를 사용하며 웹 길찾기는 목적지 정보가 담긴 네이버지도 URL을 연다.
- 실제 모바일 미리보기에서 설치 안내 시트의 안내 문구와 두 외부 링크가 표시되는 것을 확인했다. 실제 설치·외부 사이트 이동은 사용자 기기 상태를 바꾸므로 실행하지 않았다.
- 지도는 과도하게 단순화한 버전을 되돌려, 연한 로컬·주요 도로망과 수변·공원·행정 경계를 복원했다. 상업 POI·텍스트 라벨은 숨겨 이전 승인 시안의 탐색 맥락과 정돈된 화면 밀도를 함께 유지했다.
- TypeScript 검사와 Vitest **57개**가 모두 통과했다.

## 2026-08-19 — 기기별 설치 연결·최근 목적지 관리·기본 지도 복원

- 네이버 내비 미설치 안내는 사용자 기기를 판별해 Android에서는 **Google Play**, iOS에서는 **App Store** 설치 링크를 표시한다. 그 외 환경은 설치 대신 네이버지도 웹 길찾기를 기본 동선으로 제공한다.
- 출발 확인 시트의 최근 목적지 영역에 **관리** 버튼과 항목별 삭제 버튼을 추가했다. 관리 시트에서는 각 목적지 삭제와 **최근 목적지 전체 삭제**를 제공하며, 변경 내용은 `localStorage`에 즉시 반영된다.
- Route 메인 지도에서 커스텀 스타일 옵션을 제거해 기본 Google 지도 타일·도로·지명 정보 표현으로 복원했다. 실제 모바일 미리보기에서 기본 지도 표시와 길찾기 출발 확인 화면을 확인했다.
- TypeScript 검사와 Vitest **58개**가 모두 통과했다. 설치 스토어 이동은 외부 서비스 전환이므로 URL 분기와 UI 노출, Android 분기 회귀 테스트로 검증했다.

## 2026-08-19 — 최근 목적지 시각·즐겨찾기·스토어 이동 안내

- 최근 목적지에는 실제 네이버 내비 출발 시점의 UTC 타임스탬프를 브라우저 저장소에 기록하고, 목록에서는 한국 시간 기준의 월·일·시·분으로 표시한다. 이전 저장 기록에 시점 정보가 없으면 임의의 시간을 만들지 않고 `출발 기록 없음`으로 표시한다.
- 즐겨찾기 등록 항목은 보라색 하트와 강조 배경으로 구분되고, 출발 확인 시트와 관리 시트에서 항상 최근 목적지 목록 상단에 정렬된다.
- Android 설치 링크를 누르기 직전 Google Play 이동 토스트가 실행되는 것을 회귀 테스트로 확인했다. 실제 외부 스토어 전환은 브라우저 환경에 따라 수행하지 않고, 링크 분기와 안내 메시지 호출을 검증했다.
- 기본 Google 지도 화면은 브라우저 재로딩 후에도 정상 표시됐으며, TypeScript 검사와 Vitest **58개**가 통과했다.

## 2026-08-19 — 최근 목적지 최종 모바일 확인 및 길찾기 안정화

- 실제 모바일 미리보기에서 최근 목적지 두 건을 준비해, 즐겨찾기인 `오븐 성수`가 더 최근에 출발한 `성수 식당`보다 먼저 표시되고 각각 `마지막 출발 8. 19. 14:00`, `마지막 출발 8. 19. 15:01`로 표시되는 것을 확인했다.
- `성수 식당`의 하트 버튼을 눌러 즐겨찾기로 등록하자 **최근 목적지를 즐겨찾기에 고정했습니다** 토스트가 노출됐고, 해당 항목이 목록 첫 번째로 즉시 이동했다.
- 지도 장소의 길찾기 버튼에서 발견한 `moveToCurrentLocation` 초기화 순서 오류를 수정했다. 이후 동일한 모바일 진입 흐름에서 길찾기 화면과 위치 권한 안내가 정상 표시됐으며, `route:navigate-place` 이벤트 전용 회귀 테스트를 추가했다.
- 최종 `pnpm check && pnpm test` 결과 TypeScript 검사와 Vitest **59개**가 모두 통과했다.

## 2026-08-20 — 홈·마이 화면 좌우 여백 조정

- 홈과 마이 화면의 기본 콘텐츠 좌우 여백을 16px에서 **12px**로 줄여, 카드와 장소 목록이 모바일 폭을 조금 더 활용하도록 조정했다. 화면 헤더는 동일한 전체 폭을 유지하도록 음수 마진도 함께 보정했다.
- 실제 모바일 미리보기의 홈 화면에서 검색 진입 버튼, 진행 중 코스 카드, 최근 저장 장소 목록이 더 넓은 콘텐츠 폭에서도 잘림 없이 정렬되는 것을 확인했다.
- 같은 모바일 미리보기의 마이 화면에서 프로필 카드, 3개 요약 카드, 여행 관리 목록, 계정·설정 영역이 12px 좌우 여백 안에서 균형 있게 정렬되고 하단 탭과 겹치지 않는 것을 확인했다.
- 브라우저 레이아웃 측정에서도 마이 루트는 `padding-left: 12px`, 내부 프로필·요약·관리 카드의 시작 위치는 기기 프레임 기준 12px 안쪽으로 확인되어 여백 축소가 콘텐츠 전체에 반영됐다.
