# Route 지도 안내 기능 검증 기록

## 2026-08-19

- 412×900 모바일 뷰포트에서 확장된 장소 시트의 영업시간, 사진 미리보기, 상세 진입 버튼을 확인했다.
- 412×900 모바일 뷰포트에서 최근 검색어 패널의 노출과 검색 화면 배치를 확인했다.
- 실제 브라우저에서 현재 위치 버튼을 누른 뒤 geolocation 권한 거부 응답을 재현했고, **현재 위치 권한이 필요합니다** 안내 팝업과 **다시 시도**·**나중에** 버튼이 노출되는 것을 확인했다.
- `pnpm check && pnpm test` 결과 TypeScript 검사와 19개 Vitest 테스트가 통과했다.

## 2026-08-19 — Google Places 사진 및 지도 관리 기능

실제 브라우저에서 `성수 카페`를 검색해 Google Places 결과의 대표 사진이 선택 장소 프리뷰와 결과 목록에 표시되는 것을 확인했다. 각 검색 결과는 Google Places가 반환한 사진 참조를 사용하며, 제공되지 않는 경우에만 기존 기본 이미지를 사용한다.

선택한 Google Places 결과의 장소 상세 화면에서도 반환된 실제 사진이 상단 사진 영역에 적용되는 것을 확인했다. 검색 화면에서는 `성수 카페` 최근 검색어의 개별 삭제 버튼을 눌러 목록이 즉시 사라지는 것을 확인했다.

전체 삭제 동선 검증을 위해 브라우저 저장소에 두 개의 최근 검색어를 준비했다. 또한 실제 지도에서 `오븐 성수`를 저장한 뒤, 저장 장소 상태를 다시 불러와 빠른 코스 추가 버튼 노출과 중복 방지를 확인하는 흐름을 진행 중이다.

저장 후 새로고침한 실제 지도에서 `오븐 성수`를 선택하자 **현재 코스에 담김** 버튼이 표시되어 저장 장소 판별과 중복 방지가 반영된 것을 확인했다. 검색 화면에서는 `성수 맛집`과 `서울숲` 최근 검색어에 대해 **전체 삭제**를 실행했고, 최근 검색어 카드가 즉시 사라졌다.

`현재 코스에 담김` 버튼을 다시 선택했을 때 **이미 현재 여행 코스에 담긴 장소입니다** 안내가 표시되어 중복 추가가 차단되는 것을 실제 모바일 화면에서 확인했다.

빠른 코스 추가 상태를 실제로 확인하는 과정에서 `오븐 성수`를 내 장소에 임시 저장했다. 검증 후 저장 토글을 다시 실행하고 새로고침한 뒤 빠른 추가 버튼이 사라진 것을 확인하여, 이 검증용 사용자 데이터는 제거했다.

## 네이버 연결 참고

- 네이버 클라우드 공식 문서는 네이버 지도 앱의 `nmap://` URL Scheme과 장소·통합 검색 연결 방식을 안내한다. Route의 웹 환경에서는 사용자가 설치 여부와 관계없이 접근할 수 있도록 네이버 지도 웹 검색 링크를 새 창으로 열고, 장소명과 주소를 검색어로 전달한다.
- 출처: https://guide.ncloud-docs.com/docs/maps-url-scheme

## 지도 탐색 및 코스 선택 검증

- 실제 모바일 지도 화면에서 거리순·영업 중 필터가 하단 장소 시트에 노출되고, 거리 표기가 기본 지도 중심 기준으로 갱신되는 것을 확인했다.
- 장소 핀을 선택하면 장소 프리뷰의 `코스 선택` 버튼이 표시되고, 누르면 새 코스 만들기와 기존 내 여행 코스 목록을 함께 제공하는 바텀 모달이 노출되는 것을 확인했다.
- 실제 장소 상세 화면에서 사진 클릭용 확대 버튼과 `네이버에서 예약·문의` 링크가 표시되며, 링크가 네이버 지도 장소 검색 URL로 연결되는 것을 확인했다.
- 홈 화면에서 진행 중인 코스 카드가 다음 장소·시간·장소 수를 표시하고, `코스 보기`를 누르면 전체 일정·다음 장소·지도 이어가기 동선이 있는 전용 화면으로 진입하는 것을 확인했다.
- 장소 검색 화면에서 거리순·영업 중 필터가 검색 결과와 함께 유지되는 것을 확인했고, 실제 Google Places 결과의 다중 사진 갤러리를 추가 검증할 수 있도록 검색 흐름을 준비했다.
- `성수 카페` 실제 Google Places 검색에서 장소별 대표 사진·실제 평점·주소·거리와 다수의 결과가 반환되는 것을 확인했다.
- 실제 장소 상세 진입 시 Google Places Details를 추가 조회하여 최대 다섯 장의 고해상도 사진·영업 상태·전화번호를 갤러리 데이터로 보강하도록 구현했고, 관련 소스 회귀 테스트와 전체 24개 Vitest 테스트를 통과했다.
- 실제 `Cafe Onion Seongsu` 상세에서 Google Places Details 사진 5장이 로드되고, 세 장의 미리보기와 `+2` 표기가 노출되는 것을 확인했다. 두 번째 사진을 전체 화면으로 열어 `2 / 5` 상태를 확인한 뒤, 다음 사진 동작으로 `3 / 5`와 이미지 전환이 정상 반영되는 것을 모바일 화면에서 확인했다.
- 같은 갤러리에서 이전 사진 동작으로 `3 / 5`에서 `2 / 5`로 되돌아가는 것을 확인했고, 닫기 버튼을 눌러 다중 사진이 유지된 장소 상세 화면으로 복귀하는 것을 확인했다. UI 회귀 테스트에도 다중 사진의 이전·다음·닫기 단계를 추가했으며 전체 24개 테스트가 통과했다.
